---
layout: doc
docType: api
unit: GR32.ImageFormats
entity: IImageFormatAdapter
kind: Interface
declaration: "IImageFormatAdapter = interface"
summary: "Interface for image formats that handle Assign and AssignTo conversions between TCustomBitmap32 and external graphics objects."
---

## Description

`IImageFormatAdapter` provides standard bidirectional assignment mechanisms between [[TCustomBitmap32]] and external graphic objects (such as `TPicture`, `TBitmap`, `TWICImage`, or clipboard targets).

[members]
