---
layout: doc
docType: api
unit: GR32.ImageFormats
entity: IImageFormatFileReader
kind: Interface
declaration: "IImageFormatFileReader = interface"
summary: "Interface for image formats that load directly from a file path."
---

## Description

`IImageFormatFileReader` allows image format readers to utilize the filename/path directly (e.g., when leveraging native OS codecs or specialized file I/O libraries).

[members]
