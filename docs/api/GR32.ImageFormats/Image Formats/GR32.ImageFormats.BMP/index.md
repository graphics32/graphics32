---
layout: doc
docType: api
unit: GR32.ImageFormats.BMP
entity: GR32.ImageFormats.BMP
kind: Unit
summary: "Provides bitmap image format adapter and reader support for standard BMP files."
---

## Description

The `GR32.ImageFormats.BMP` unit implements image format adapters and readers for standard Windows/LCL bitmap files (`.bmp`).

---

[members]
