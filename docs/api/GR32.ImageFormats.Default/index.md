---
layout: doc
docType: api
unit: GR32.ImageFormats.Default
entity: GR32.ImageFormats.Default
kind: Unit
summary: "Registers default built-in image format handlers and adapters."
---

## Description

The `GR32.ImageFormats.Default` unit automatically registers default image format adapters, readers, and writers with [[ImageFormatManager]] during initialization.

---

[members]
