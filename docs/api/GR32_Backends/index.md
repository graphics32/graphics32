# Unit GR32_Backends

The `GR32_Backends` unit provides surface backend implementations abstraction layer for `TBitmap32`.

---

## Classes

- `TGDIBackend`: Windows GDI device context surface backend.
- `TMMFBackend`: Memory-mapped file surface backend.
