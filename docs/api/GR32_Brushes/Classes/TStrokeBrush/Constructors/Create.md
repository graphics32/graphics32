---
layout: doc
docType: api
unit: GR32_Brushes
parent: TStrokeBrush
entity: TStrokeBrush.Create
kind: Constructor
declaration: "constructor Create(BrushCollection: TBrushCollection); override;"
summary: "Creates and initializes a TStrokeBrush instance with default stroke width and miter limit."
---

## Description

`Create` initializes a new `TStrokeBrush` instance with [[StrokeWidth]] set to `1.0`, [[FillMode]] set to `pfWinding`, and [[MiterLimit]] set to `DEFAULT_MITER_LIMIT`.
